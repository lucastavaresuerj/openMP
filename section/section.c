#include <stdlib.h>
#include <stdio.h>

double soma(double ** mat, int linha){
    int tamanho = sizeof(mat[0])/sizeof(mat[0][0]);
    double media = 0;
    for(int i = 0; i<tamanho; i++){
        media = mat[linha][i] / ;
    }
}